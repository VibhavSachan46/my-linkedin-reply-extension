// wxt.config.js
import { defineConfig } from './entrypoints/background.ts';

export default defineConfig({
  background: 'path/to/your/background.ts', // Adjust the path as necessary
  // Other configurations...
});
